package com.bjsxt.scala

object Scala_List {
  def main(args: Array[String]): Unit = {
//    val list = List[Int](1, 2, 3) // List 可是不指定类型，但是最好指定
////    list.foreach(println)
//
//    // 过滤出符合条件的元素，返回一个新的 List
//    val flist = list.filter(value=>{
//      value < 3
//    })
////    flist.foreach(println)
//
//    // 计算符合条件的元素的个数
//    val num = list.count(value=>{
//      value > 2
//    })
//    println(num)


    val str = 'str
    val str1 = Symbol("str")
    println(str == str1)
  }
}
